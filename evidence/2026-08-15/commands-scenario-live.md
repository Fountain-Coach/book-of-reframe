# `/commands` scenario acceptance

Scenario: `commands-discover`
Capability: `app.commands.discover`
Status: `live-accepted`

The executable scenario was run three times in one fresh managed Store. Each run reached the terminal capability
phase `succeeded`; the independent witness observed the `/commands` result through the AX tree and captured the
Reframe window by CoreGraphics window ID.

The sanitized result surface exposed the current local catalog: 82 available entries, 5 unavailable entries, and 3
visible only in the compatibility `//` section. The older Ulysses catalog remains historical inventory evidence and
is not used as proof for this scenario.

Evidence binding: isolated managed Store; Reframe source commit `cefee09c`; Reframe window capture 1920×1050;
AX identifiers `studio-chat-input`, `studio-chat-send`, and the resulting `chat-row-message-*` / command-result
surface. Private process and Store identifiers remain outside this public projection.

The scenario is now the acceptance authority for the command page; legacy evidence remains linked only as history.
