# Historical and development command inventory

The 95 command entries are retained because they describe the current development runtime and its accumulated
compatibility surface. They include aliases, help commands, maintainer-only UI-learning entries, local orchestration,
and commands whose availability depends on live preconditions.

They are not a list of released App capabilities. A command enters the release surface only through a governed
capability identity and a named release manifest.
